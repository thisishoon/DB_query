with recursive cte as(select employee_worker from manager_worker where employee_manager=(select employee_id from employee where employee_name="Hong GilDong") union all select manager_worker.employee_worker from manager_worker inner join cte on cte.employee_worker=manager_worker.employee_manager) select employee_name from employee where employee_id in (select employee_worker from cte);